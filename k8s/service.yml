apiVersion: v1
kind: Service
metadata:
  name: wawa
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wawa"
    port: 8080
  selector:
    app: wawa
